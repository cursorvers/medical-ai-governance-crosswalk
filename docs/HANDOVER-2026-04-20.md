# Handover — 2026-04-20 後半

## 決定事項 (軽量路線)

- **v0.1 pilot は延期**: 3 lane critique (I/J/K) で NO-GO/PIVOT 判定、軽量化路線に転換
- **GuideScope との関係**: 独立 MCP server を作らず、corpus を npm package として export し GuideScope 本体に統合
- **コア価値**: 「PMDA相談前の埋めとけシート」を FDA/EU/WHO 根拠リンク付きで出す (evidence pack)
- **削る**: diff-bot / reviewer network / telemetry / 独自 MCP server (重すぎ・運用負担)

## 次の実行タスク (承認済み、並列)

### Task 1: v0.1.1 defensive 修繕 (Lane I 赤6件)

Codex 委譲。以下を修正:

1. `scripts/validate_corpus.py --check-urls`: ISO 403 / METI timeout / NIST 404 を正直に exit 非ゼロ化 (retry + 明示 skip リスト)
2. `source_hash: sha256:unavailable` を validator で reject (64文字 hex 強制)
3. 深リンク化: FDA / EU / PMDA / WHO / NIST の 5本 × 13 列 = 65 cell をトップ URL → 節/段落アンカー URL に更新
4. JMSF (`jmsf-ai-medical.yml`): 全13 cell `source_unavailable` → 日本医師会 2026-04-15 答申 (https://www.med.or.jp/dl-med/teireikaiken/20260415_3.pdf) に差し替え
5. `scripts/check_urls.py`: 月次 CI で `--strict` 使用、失敗で exit 非ゼロ
6. README: 「基準書」「そのまま引用可能」トーン → 「read-order map / 引用入口」に修正

### Task 2: evidence pack v1 (SaMD事前相談準備シート)

Codex 委譲。以下を実装:

- `templates/samd-consultation-prep.md.j2`: jinja2 テンプレート (PMDA SaMD 事前相談用)
- `scripts/generate_evidence_pack.py`: `corpus/guidelines/*.yml` から関連 cell を抽出し、`build/evidence/samd-consultation-prep.md` を生成
- `site/docs/evidence.md`: ダウンロードページ (GitHub Pages で静的配信、サーバー不要)
- 対象 column: A (applicability) / B (risk) / G (PCCP_postmarket) / K (clinical_evaluation) / M (lifecycle)

## 保留中 (承認ゲート)

- GitHub remote push (cursorvers-inc/medical-ai-governance-crosswalk)
- gh-pages deploy
- npm publish (`@cursorversinc/guidescope-medical-corpus`)
- X 告知

各々 `/vote` 経由で個別承認。

## 参照 critique

- `/tmp/fugue-medgov-2026-04-20/critique-I-tech.md` (Codex, NO-GO)
- `/Users/masayuki/Dev/medical-paper-governance/critique-J-market.md` (Copilot, PIVOT)
- `/tmp/fugue-medgov-2026-04-20/critique-K-competitive.md` (Codex, High novelty × Low moat)
